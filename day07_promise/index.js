
// Promise

// They will be fullfilled
// They will be not fullfilled
// They will be in process

var Promise = new Promise();

// Resolve()

// Reject()


var mypromise = new Promise(function(resolve,reject) {
    resolve('success');
    reject('error');
})

